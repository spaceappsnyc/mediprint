'use strict';

describe('myApp.conditions module', function() {

  beforeEach(module('myApp.conditions'));

  describe('conditions controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var conditionsCtrl = $controller('ConditionsCtrl');
      expect(conditionsCtrl).toBeDefined();
    }));

  });
});
